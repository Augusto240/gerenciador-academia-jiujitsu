source 'https://rubygems.org'

gem 'sinatra'
gem 'pg'
gem 'bcrypt'
gem 'connection_pool'
gem 'puma'
gem 'bigdecimal'
gem 'rackup'
gem 'rake'

# Segurança
gem 'rack_csrf'        # Proteção contra CSRF
gem 'rack-attack'      # Rate limiting e proteção contra brute force

group :test do
  gem 'minitest'
  gem 'rack-test'
end
